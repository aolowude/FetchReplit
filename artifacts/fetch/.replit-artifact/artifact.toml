kind = "web"
previewPath = "/"
title = "Fetch"
version = "1.0.0"
id = "artifacts/fetch"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 24894

[services.development]
run = "pnpm --filter @workspace/fetch run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/fetch", "run", "build" ]
publicDir = "artifacts/fetch/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "24894"
BASE_PATH = "/"
